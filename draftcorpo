<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Corporate Drafting Portal | Legal & Accounting</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-blue: #123765;
            --light-blue: #5f98b6;
            --gold: #ccaa49;
            --dark-bg: #1a2634;
            --white: #ffffff;
        }

        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            background-color: #f4f7f9;
            color: var(--dark-bg);
        }

        /* Navigation */
        nav {
            background-color: var(--primary-blue);
            padding: 1rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }

        .logo { font-weight: 600; font-size: 1.5rem; letter-spacing: 1px; }

        /* Main Container */
        .container { padding: 2rem 5%; max-width: 1200px; margin: auto; }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            margin-bottom: 2rem;
        }

        /* Tabs */
        .tabs { display: flex; gap: 10px; margin-bottom: 20px; }
        .tab-btn {
            padding: 10px 20px;
            border: none;
            background: #e0e0e0;
            cursor: pointer;
            border-radius: 5px;
            font-weight: 600;
        }
        .tab-btn.active { background: var(--gold); color: white; }

        /* Status Bar */
        .status-bar {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            background: #eee;
            border-radius: 50px;
            padding: 5px;
        }
        .status-item { padding: 10px 15px; border-radius: 50px; font-size: 0.8rem; }
        .status-item.active { background: var(--light-blue); color: white; }

        /* Chat Assistance */
        #chat-widget {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
        }
        .chat-btn {
            background-color: var(--gold);
            color: white;
            border: none;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            cursor: pointer;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
            font-size: 1.5rem;
        }
        .chat-popup {
            display: none;
            position: absolute;
            bottom: 70px;
            right: 0;
            width: 300px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 5px 25px rgba(0,0,0,0.2);
            overflow: hidden;
        }
